# Kopieer naar .env en pas aan (niet committen).
# Zelfde waarde op weegbrug-PC (WEEGSERVER_KEY) en kantoor-PC (VITE_WEEGSERVER_KEY).

VITE_WEEGSERVER_KEY=BultersWs-8kM2pQ9v

# Optioneel op weegbrug-PC — komma-gescheiden IP's van kantoor-PC's:
# WEEGSERVER_ALLOWED_IPS=192.168.1.20,192.168.1.21
